<% if(entries){ %>
<%_ entries.forEach(function(entry) { _%>

  <%- include('../partials/everyoneEntryCard', {entry: entry, groupings: groupings, userData: userData, userFollowing: userFollowing}) %>

<%_ }) _%>
<% } else { %>
<p>No entries to show!</p>
<% } %>
